// The Call for Papers is closed for JSConf Brasil 2026. Set this back to `true` to reopen
// submissions (or swap in a date-based check like configs/vote.ts's `isVotingOpen`).
export const C4P_OPEN = false;

export const isC4pOpen = (): boolean => C4P_OPEN;
