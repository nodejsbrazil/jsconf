{
  "name": "jsconf-br-waitlist-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "db:init": "wrangler d1 execute jsconf-br-waitlist --file=schema.sql",
    "db:init:remote": "npm run db:init -- --remote",
    "test": "poku"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "poku": "^4.1.0",
    "tsx": "^4.21.0",
    "wrangler": "^4.0.0"
  }
}
